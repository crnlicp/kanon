1. In admin, All backend operations such as saving forms or fetching data in some pages gives error. Check what is wrong with that.
2. In Admin when we don't have records in different pages, The UI for showing "there is no data" is not identical. Make sure all "missing data" UI are the same for all pages.
3. In "dfx deploy there is a warning. Find the issue and solve it. 

Plan carefully step by step to find out and solve the issues. At the end try to deploy and make sure there is no errors and warnings. Command for deploy is "dfx deploy". 